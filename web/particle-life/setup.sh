#!/usr/bin/env bash
# setup.sh — Particle Life (static)
# No dependencies — just open index.html in a browser.
set -euo pipefail

echo "🦠 Particle Life — static project"
echo "   No setup needed."
echo "   Open index.html in a browser to run."
